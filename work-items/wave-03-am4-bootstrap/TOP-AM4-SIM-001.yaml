id: TOP-AM4-SIM-001
kind: implementation
epic: TOP-1200
title: Replay AM4 identity discovery through the simulator and registry
status: BLOCKED_FIXTURE
priority: high
requirement_ids:
- SIM-002
- DEV-001
depends_on:
- TOP-AM4-PROTO-001
- TOP-AM4-PROTO-002
- TOP-AM4-REG-001
- TOP-SIM-001
parallel_group: am4-sim-a
agent:
  preferred_role: topology_implementer
  reviewer_role: topology_reviewer
  integrator_role: parent_orchestrator
scope:
  read:
  - fixtures/am4/identity/**
  - crates/topology_protocol_am4/**
  - crates/topology_device_registry/**
  - crates/topology_simulator/**
  write:
  - crates/topology_simulator/tests/am4_discovery.rs
  - crates/topology_simulator/fixtures/am4_identity_script.*
  - .tdd/evidence/TOP-AM4-SIM-001/**
  forbidden:
  - Cargo.toml
  - Cargo.lock
  - apps/**
  - native/**
  - device-packs/**
  - work-items/index.yaml
  - docs/requirements/traceability.yaml
behavior:
  why: The first device-specific path should compose query, response, identity, and exact profile resolution before hardware UI work.
  given:
  - Simulator script contains approved AM4 exchange.
  - Registry has exact profile.
  when:
  - Discovery session executes.
  then:
  - Exact query sent.
  - Response decoded.
  - Exact profile selected.
  - Session exposes only declared capabilities.
  - Transcript identifies simulator.
non_goals:
- Physical AM4
- Native transport
- Preset read
preconditions:
- AM4 codec/profile and generic simulator integrated.
test:
  layer: simulator_integration
  file: crates/topology_simulator/tests/am4_discovery.rs
  name: am4_discovery_replay_selects_exact_profile
  red_command: cargo test -p topology-simulator am4_discovery_replay_selects_exact_profile -- --exact --nocapture
  expected_red:
    acceptable:
    - Missing composed discovery flow.
    - Assertion fails at query/decode/profile.
    unacceptable:
    - Test bypasses codec or registry.
    - Fixture checksum invalid.
  green_command: cargo test -p topology-simulator am4_discovery_replay_selects_exact_profile -- --exact --nocapture
minimum_green:
- Wire existing components.
- Record transcript.
- No native/hardware code.
required_sweeps:
- cargo test -p topology-protocol-am4
- cargo test -p topology-device-registry
- cargo test -p topology-simulator
- cargo fmt --all -- --check
acceptance:
- Full simulator discovery passes.
- Unknown firmware adjacent scenario read-only.
- Reviewer approves.
available_claims_after_completion:
- SIMULATOR_VERIFIED
- BYTE_FIXTURE_VERIFIED
unavailable_claims:
- PLATFORM_DEVICE_VERIFIED
- HARDWARE_VERIFIED
required_followups:
- TOP-AM4-HIL-001
- TOP-AM4-E2E-001
block_conditions: []
evidence_directory: .tdd/evidence/TOP-AM4-SIM-001
amendments:
- recorded_at: 2026-08-13T01:35:16-05:00
  author: parent_orchestrator
  reason: All non-device prerequisites are integrated; the remaining direct prerequisites are the lawful AM4 identity fixture, exact decoder, and exact profile, each rooted in user-supplied hardware capture and redistribution approval.
  changes:
  - status changes from BLOCKED_DEPENDENCY to BLOCKED_FIXTURE.
  - no simulator, protocol, profile, or compatibility behavior is written or claimed.
  claim_changed: false
