Status: `GREEN_OBSERVED`; independent review pending.

Created only the pinned Rust workspace, one empty domain crate, one linkage
harness test, and the generated lockfile. No product type, dependency, future
crate, protocol, fixture, or hardware claim was added.
