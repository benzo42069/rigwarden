status=CANDIDATE_REVIEW_APPROVED_INTEGRATION_PENDING
work_item=TOP-REG-002
dependency=TOP-REG-001 is `INTEGRATED` at recorded integration base `a24244cd849658f9758adbf86c7b505ec8b9bf65`; current shared worktree HEAD is pre-existing dirty integration material.
behavior=Known family/model with unprofiled firmware resolves to an explicit read-only result without nearest/lower firmware fallback.
files_changed=crates/topology_device_registry/src/resolve.rs; crates/topology_device_registry/tests/unknown_firmware.rs; .tdd/evidence/TOP-REG-002/**
design=Kept the REG001 `DeviceRegistry::resolve` exact-only Option API unchanged. Added `resolve_session`, which returns the exact result when available and otherwise synthesizes a read-only `ResolvedProfile` only when family/model are known. Added `ResolutionProvenance::UnknownFirmware` and Copy-compatible unit `ResolutionStatus::UnknownFirmware`; `ResolvedProfile::unsupported_firmware()` carries the observed unsupported FirmwareId as the machine-readable reason. The synthesized profile preserves observed family/model/firmware and uses false write capability plus `VerificationStatus::ReadOnly`.
compatibility_assessment=`ResolutionStatus` remains `Clone + Copy` and `ResolvedProfile::status()` remains `pub const fn`, preserving REG001 status-call compatibility; the new firmware reason is additive through `unsupported_firmware()` and does not overload the status enum.
red=cargo test -p topology-device-registry unknown_firmware_never_inherits_write_capability -- --exact --nocapture exited 101 before production edits for the intended missing session-result API/status; see red.log and red-exit-status.txt. The test was adapted to existing exports because lib.rs is forbidden; a supplementary final-shape RED after temporarily removing only the additive implementation also exited 101 (red-final-api.log/red-final-api-exit-status.txt), then source was restored.
green=cargo test -p topology-device-registry unknown_firmware_never_inherits_write_capability -- --exact --nocapture exited 0; compatibility-preserving final API cycle also exited 0 in green-compat-api.log and green-after-compat-format.log.
sweeps=Initial required sweeps all exited 0 in sweep.log/sweep-exit-statuses.txt; supplementary restored-source sweeps passed in sweep-after-final-api-red.log; first compatibility sweep stopped at a formatter-only diff and is preserved in sweep-after-compat-api.log; final fail-fast required sweep passed all four commands in sweep-final.log/sweep-final-exit-statuses.txt.
claims_earned_candidate=Independent review is `REVIEW_APPROVED`; focused/unit behavior is GREEN and the final required sweeps pass. `UNIT_VERIFIED` and `READ_ONLY` become available after the parent integration rerun from an immutable integration commit; they are not yet integrated claims in this shared worktree.
claims_unavailable=BYTE_FIXTURE_VERIFIED; HARDWARE_VERIFIED; protocol/transport/simulator/platform/accessibility/UI claims.
shared_changes_proposed=none; lib.rs was intentionally not edited because it was forbidden by the packet and existing exports suffice.
fixture_source=synthetic typed in-memory values; no protocol fixture or external source used.
pitfalls=The shared worktree is pre-existing and dirty, and REG001 source is present as untracked integration material while index metadata says INTEGRATED. The candidate.patch therefore records full current resolve.rs content for reference. Parent/integration owner must preserve other agents' changes and independently rerun the packet sweep.
next_packet=TOP-CMD-002 after TOP-REG-002 integration and TOP-CMD-001 dependency.
blockers=None for this executable pure-Rust candidate; independent review and integration rerun remain required before status advancement.
