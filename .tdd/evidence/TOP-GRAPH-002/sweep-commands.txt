1. cargo test -p topology-routing valid_serial_connection_is_queryable_from_both_ends -- --exact --nocapture (post-format focused rerun; exit 0)
2. cargo test -p topology-routing (adjacent package suite; exit 0)
3. cargo fmt --all -- --check (initial formatter probe; exit 1, mechanical layout only)
4. cargo fmt --all (mechanical formatter correction; exit 0)
5. cargo fmt --all -- --check (final formatter sweep; exit 0)
6. cargo clippy -p topology-routing --all-targets -- -D warnings (lint sweep; exit 0)
