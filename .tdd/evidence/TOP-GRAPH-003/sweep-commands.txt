1. cargo test -p topology-routing (adjacent package suite; exit 0)
2. cargo fmt --all -- --check (format sweep; exit 0)
3. cargo clippy -p topology-routing --all-targets -- -D warnings (lint sweep; exit 0)
